# Django Settings
SECRET_KEY=django-insecure-change-this-in-production-xyz123abc456
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Database (Leave empty to use SQLite for development)
# For PostgreSQL on Railway:
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Redis (Optional for development - leave empty to use in-memory)
# For Railway Redis:
# REDIS_URL=redis://default:password@host:port

# Email Configuration (Optional)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@school.com

# Railway Deployment (Set automatically by Railway)
# RAILWAY_ENVIRONMENT=production
# PORT=8000

# M-Pesa Configuration (Optional - for fee payments)
MPESA_CONSUMER_KEY=
MPESA_CONSUMER_SECRET=
MPESA_SHORTCODE=
MPESA_PASSKEY=
MPESA_CALLBACK_URL=
